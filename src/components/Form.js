"use strict";

var React = require('react');

var Form = React.createClass({
  render: function() {
    return (
      <div>
        <p>I should be a form</p>
      </div>
    );
  }
});

module.exports = Form;
