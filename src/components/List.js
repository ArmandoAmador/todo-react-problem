"use strict";

var React = require('react');

var List = React.createClass({
  render: function() {
    return (
      <div>
        I should be a list of tasks
      </div>
    );
  }
});

module.exports = List;
